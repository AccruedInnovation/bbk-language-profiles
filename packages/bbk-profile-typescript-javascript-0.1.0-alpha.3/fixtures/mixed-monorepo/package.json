{
  "name": "fixture-mixed-monorepo",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "workspaces": ["packages/*"],
  "scripts": {"build": "pnpm -r build", "test": "pnpm -r test", "typecheck": "pnpm -r typecheck"}
}
