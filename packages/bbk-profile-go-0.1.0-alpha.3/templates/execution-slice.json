{
  "assertions": [
    {
      "evidence": "golden CLI output and digest",
      "id": "A-S1-ORDER",
      "method": "test",
      "statement": "The same fixture and order produce identical output bytes."
    },
    {
      "evidence": "spy repository invocation count and diagnostic",
      "id": "A-S1-INVALID",
      "method": "test",
      "statement": "Unknown order text is rejected before repository access."
    }
  ],
  "atomicity": {
    "coherent": true,
    "containedOrReversible": true,
    "independentlyVerifiable": true,
    "rationale": "The slice closes one end-to-end read path without durable mutation.",
    "reviewable": true
  },
  "dependencies": [],
  "entryConditions": [
    "ISC-SOFTWARE-001 revision 1 accepted."
  ],
  "exitConditions": [
    "CLI touchpoint executes.",
    "Assertions A-S1-ORDER and A-S1-INVALID pass.",
    "No adapter-owned ordering rule exists."
  ],
  "flow": {
    "architectureRefs": [
      "ARCH-DOMAIN",
      "ARCH-APPLICATION",
      "ARCH-CLI"
    ],
    "interfaceRefs": [
      "IF-DECISION-QUERY"
    ],
    "participants": [
      "CLI adapter",
      "Query service",
      "Fixture repository"
    ],
    "steps": [
      "Parse external text into a closed DecisionOrder.",
      "Read fixture entries through the repository port.",
      "Return and render a deterministic DecisionIndex."
    ]
  },
  "integrationOwner": "Worker Orchestrator: decision query",
  "metadata": {
    "example": true
  },
  "objective": "Create the typed query/result path and an executable CLI touchpoint backed by a deterministic fixture repository.",
  "parentCapabilityRefs": [
    "CAP-QUERY-DECISIONS"
  ],
  "profileProjections": {
    "python": {
      "expectedArtifacts": [
        "decision_index.py",
        "query_service.py",
        "test_decision_index.py"
      ]
    }
  },
  "scaffolding": [
    {
      "disposition": "retain-as-fixture",
      "id": "SC-FIXTURE-REPO",
      "owner": "test",
      "purpose": "Provide deterministic entries before the production repository adapter is connected."
    }
  ],
  "schema": "bbk.execution-slice.v1",
  "sliceId": "ES-SOFTWARE-001",
  "status": "ready",
  "structureContractRefs": [
    "ISC-SOFTWARE-001@1"
  ],
  "title": "Typed query and executable CLI fixture",
  "touchpoint": {
    "actor": "developer/reviewer",
    "environment": "offline fixture environment",
    "expectedObservation": "Ordered decision identities are rendered consistently and invalid order text is rejected.",
    "interaction": "Run bbk decisions list against the fixture repository.",
    "kind": "cli"
  },
  "workUnitRefs": [
    "WU-DOMAIN-TYPES",
    "WU-QUERY-SERVICE",
    "WU-CLI-FIXTURE"
  ]
}
