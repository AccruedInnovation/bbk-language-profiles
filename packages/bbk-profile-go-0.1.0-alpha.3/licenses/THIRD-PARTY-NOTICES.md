# Third-party notices

This package contains the user-supplied `comprehensive-analysis-go` skill in adapted form. Its original byte digest is recorded in `sources/original-digests.json`.

The package does not bundle the Go toolchain or any external analyzer. Names such as Go, `govulncheck`, Staticcheck, golangci-lint, `go test`, `gofmt`, and `testing/synctest` identify optional ecosystem tools or standard-library capabilities and do not imply bundled code or endorsement.
